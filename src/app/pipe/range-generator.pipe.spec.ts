import { RangeGeneratorPipe } from './range-generator.pipe';

describe('RangeGeneratorPipe', () => {
  it('create an instance', () => {
    const pipe = new RangeGeneratorPipe();
    expect(pipe).toBeTruthy();
  });
});
